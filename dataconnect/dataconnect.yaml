specVersion: "v1beta"
serviceId: "concertz"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "concertz-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
